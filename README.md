# Optimal Vehicular Federated Learning

## Setup Python
```bash
pip -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```
Once installed all the required dependencies run the game with 
```bash
python main.py
```
